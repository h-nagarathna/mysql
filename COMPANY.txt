CREATE TABLE DEPT(
DID INT AUTO_INCREMENT PRIMARY KEY,
DNAME VARCHAR(30) UNIQUE NOT NULL,
DEMAIL VARCHAR(40) UNIQUE,
DPHONE BIGINT UNIQUE NOT NULL CHECK(LENGTH(DPHONE)=10),
DHOD VARCHAR(30),
PRINCIPAL VARCHAR(20) DEFAULT "MAHESH");

ALTER TABLE DEPT
ADD SUBJECT VARCHAR(20) NOT NULL;

ALTER TABLE DEPT
ADD SLNO INT AFTER DPHONE;

ALTER TABLE DEPT
ADD USN BIGINT FIRST;

-
CREATE TABLE COMPANY(
EID INT PRIMARY KEY AUTO_INCREMENT,
EMPOLYEE VARCHAR(20) NOT NULL,
DEPT VARCHAR(30) UNIQUE,
PROJECT VARCHAR(30) NOT NULL UNIQUE);

ALTER TABLE COMPANY
ADD LOCATION VARCHAR(50) NOT NULL;

ALTER TABLE COMPANY
ADD SALARY INT AFTER DEPT;

ALTER TABLE COMPANY
ADD PHONE BIGINT UNIQUE CHECK(LENGTH(PHONE)=10) FIRST;




